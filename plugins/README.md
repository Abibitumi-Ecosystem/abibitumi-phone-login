# Plugins

This directory is reserved for Abibitumi authentication plugins.

- `abibitumi-id/` - identity layer, shared verification source of truth, and
  phone OTP login.

Keep non-authentication products, including chat/support tooling, out of this
repository.
